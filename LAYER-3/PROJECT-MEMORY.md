# PROJECT-MEMORY — Долгая память проекта

> Этот файл агент читает при каждой сессии ПОСЛЕ HANDOFF.md.
> Здесь накапливается знание о проекте которое не теряется между сессиями.
> Обновляется агентом автоматически при появлении новых паттернов или ошибок.

---

## Общая информация

- **Проект:** (заполнить)
- **Стек:** (заполнить)
- **Архитектура:** (заполнить одной строкой — например: «SPA + REST API + PostgreSQL»)

---

## Что уже реализовано

> Список готового функционала. Агент добавляет пункт после каждой закрытой задачи.

- Трёхслойная структура документации шаблона: `LAYER-1/` (инструкции агента), `LAYER-2/` (ТЗ), `LAYER-3/` (память); точка входа `llms.txt`; скрипты в `LAYER-1/tools/`.

---

## Что НЕ работает / известные ограничения

> Сюда идут не баги, а принятые ограничения системы.
> Например: «авторизация через email, OAuth не планируется в MVP»

- (пока пусто)

---

## Частые ошибки в этом проекте

> Паттерны ошибок которые повторялись больше одного раза.
> Агент добавляет сюда после второго вхождения одной и той же ошибки.

| Ошибка | Где встречалась | Как решали |
|--------|----------------|-----------|
| (пока пусто) | | |

---

## Паттерны решений

> Подходы которые сработали и стоит повторять.
> Агент добавляет после успешного закрытия нетривиальной задачи.

- (пока пусто)

---

## Важные решения (краткий журнал)

> Не полный лог, а только решения которые повлияли на архитектуру или стек.
> Полные обоснования — в LAYER-2/specs/decisions.md.

| Дата | Решение | Почему |
|------|---------|--------|
| (пока пусто) | | |

---

## Правила обновления этого файла

Агент обновляет этот файл когда:
- Закрыта задача → добавить в «Что уже реализовано»
- Ошибка встретилась второй раз → добавить в «Частые ошибки»
- Принято архитектурное решение → добавить в «Важные решения»
- Найден рабочий паттерн → добавить в «Паттерны решений»

Агент НЕ чистит этот файл самостоятельно — только владелец.
