---
task:
  id: "task-012"
  goal: "Подготовить AgentOS Release Packaging для установки guardrails в другие проекты"
  expected_result: "install.sh, templates, examples и docs позволяют безопасно установить minimal/full AgentOS templates"
  in_scope:
    - "install.sh"
    - "templates/agentos-minimal/"
    - "templates/agentos-full/"
    - "examples/simple-project/"
    - "docs/quickstart.md"
    - "docs/usage.md"
    - "tasks/active-task.md"
    - "reports/verification.md"
  out_of_scope:
    - "pip package"
    - "npm package"
    - "Docker image"
    - "Homebrew packaging"
    - "GUI"
    - "backend"
    - "web service"
    - "marketplace integration"
    - "deployment"
    - "GitHub release"
    - "Git history changes"
    - "Risk Guard changes"
    - "Dangerous Command Guard changes"
    - "Git hooks changes"
    - "PR Quality Gate changes"
    - "scripts/run-all.sh changes"
    - "README.md"
    - "HANDOFF.md"
    - "memory-bank/project-status.md"
  files_or_areas:
    - "install.sh"
    - "templates/"
    - "examples/"
    - "docs/"
    - "tasks/"
    - "reports/"
  risk_level: "MEDIUM"
  risk_reason: "Добавляется installation/packaging слой, который копирует файлы в другие git-проекты, но не меняет guardrail logic"
  requires_owner_approval: false
  owner_approval_proof: ""
  rollback_plan: "Remove install.sh, templates/agentos-minimal, templates/agentos-full, examples/simple-project and docs packaging files if packaging causes issues"
  acceptance_criteria:
    - "install.sh exists and is executable"
    - "install.sh uses SCRIPT_DIR for template path resolution"
    - "templates/agentos-minimal exists"
    - "templates/agentos-full exists"
    - "examples/simple-project exists"
    - "docs/quickstart.md exists"
    - "docs/usage.md exists"
    - "install.sh supports --dry-run --minimal --full --force --help"
    - "install.sh refuses to run outside git repository root"
    - "install.sh does not overwrite files without --force"
    - "install.sh copies minimal template by default"
    - "install.sh --dry-run does not copy files"
    - "quickstart explains minimal and full install"
    - "usage explains common commands"
    - "simple-project contains README.md app.py requirements.txt"
    - "python3 scripts/validate-task.py tasks/active-task.md exits with code 0"
    - "python3 scripts/validate-verification.py reports/verification.md exits with code 0"
  verification_plan:
    - "test -x install.sh"
    - "bash install.sh --help"
    - "bash install.sh --minimal --dry-run"
    - "bash install.sh --full --dry-run"
    - "test -d templates/agentos-minimal"
    - "test -d templates/agentos-full"
    - "test -d examples/simple-project"
    - "test -f docs/quickstart.md"
    - "test -f docs/usage.md"
    - "python3 scripts/validate-task.py tasks/active-task.md"
    - "python3 scripts/validate-verification.py reports/verification.md"
---
