# detected-state-failed

Broken rule:
- Case requires capabilities not safely controllable in current MVP without validator path overrides.

Expected:
- status: SKIPPED
- reason: detect-task-state source control for specific state requires validator-path/input-model overrides in current MVP

Notes:
- This fixture is intentionally skipped in current MVP.
