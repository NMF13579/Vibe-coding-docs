# missing-approval-validator-unreliable-direct-checks

Broken rule:
- Case requires capabilities not safely controllable in current MVP without validator path overrides.

Expected:
- status: SKIPPED
- reason: missing approval validator simulation requires validator-path override, out of scope in 12.6.1

Notes:
- This fixture is intentionally skipped in current MVP.
