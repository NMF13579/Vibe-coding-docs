{
  "case": "session-parent-traversal-verification",
  "target": "verification",
  "command": "verification runner must reject parent traversal session path",
  "expected_result": "NOT RUN",
  "expected_exit_code": 2,
  "must_not_create_session": true,
  "must_not_modify_fixture_inputs": true,
  "notes": "verification runner must reject parent traversal session path"
}
